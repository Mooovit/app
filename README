To build the apk :

Prerequisites :
```
brew install bundletool
keytool -genkeypair -v -storetype PKCS12 -keystore my-upload-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000
mv my-upload-key.keystore android/app
```

Build :

```
./gradlew bundleRelease
bundletool install-apks --apks=relesae.zip
```

MISC
```
bundletool build-apks --bundle=app-release.aab --output=relesae.apks --ks=/Users/matthieutirelli/dev/perso/app/android/app/my-upload-key.keystore --ks-key-alias=my-key-alias --ks-pass=pass:totototoadb install -l app-debug.apk
```
